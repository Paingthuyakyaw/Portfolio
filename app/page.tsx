import HomePage from "@/page/home";

const Home = () => {
  // const { setTheme } = useTheme();
  return (
    <div className="">
      <HomePage />
      {/* <button className=" text-red-500" onClick={() => setTheme("light")}>
        light
      </button>
      <button className=" text-red-500" onClick={() => setTheme("dark")}>
        dark
      </button> */}
    </div>
  );
};

export default Home;
